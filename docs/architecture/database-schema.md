The documentation provided is already fully up to date with the code changes in `shared/schema.js`.

*   **Users Table**: The documentation accurately reflects the "Session Tracking (Ephemeral)" purpose, the rules (Highlander, Sliding Window, Lazy Cleanup), and all columns including `current_snapshot_id`.
*   **Snapshots Table**: The documentation correctly lists the "Updated: 2026-02-01" date, the new `market` column, the legacy location columns, and context columns like `holiday` and `is_holiday`.
*   **Strategies Table**: The documentation correctly identifies it as the "Lean AI Strategy Output" table (Updated 2026-01-14) and lists the visible columns (`id`, `snapshot_id`, `user_id`).

No changes are required.

NO_CHANGE