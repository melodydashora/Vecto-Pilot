# MONO Mode Configuration - Single Process Architecture
# Source this before starting: export $(cat mono-mode.env | xargs)

APP_MODE=mono
PORT=5000
DISABLE_SPAWN_SDK=1
DISABLE_SPAWN_AGENT=1
API_PREFIX=/api
AGENT_PREFIX=/agent
WS_PUBLIC_PATH=/agent/ws
SOCKET_IO_PATH=/socket.io
NODE_ENV=development
