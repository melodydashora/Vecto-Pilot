entrypoint = "gateway-server.js"
modules = ["nodejs-22", "postgresql-16"]

[nix]
packages = ["nodejs-22"]
channel = "stable-25_05"

[deployment]
build = ["sh", "-c", "npm ci --omit=dev && npm run build"]
run = ["sh", "-c", "PORT=${PORT} npm start"]

[[ports]]
localPort = 24678
externalPort = 80
