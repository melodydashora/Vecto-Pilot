# Merge Log: Server Lib + DB + Jobs + Logger + Validation + Scripts + Server Config

**Merge:** HEAD `1960cd41` ← incoming `d39d570f` ("Daily 8-12hr strategy removed end-to-end")
**Resolved by:** Claude Code (Opus 4.7)
**Date:** 2026-04-28
**Scope:** 49 assigned files

## Summary

| Category | Count |
|----------|------:|
| Total assigned files | 49 |
| Resolved by TAKE INCOMING | 36 |
| Resolved by DELETE | 1 |
| Resolved by KEEP (incoming via worktree on DU) | 1 |
| Auto-resolved (identical or non-conflicting) | 11 |
| Punted (needs Melody review) | 0 |

**Net result:** all 49 files staged. `git status --short server/ \| grep -E "^(AA\|UU\|UD\|DU)"` returns empty.

## Doctrine Notes

The incoming branch represents a substantial forward step:
- **Daily 8-12hr strategy removed** — `consolidated_strategy` references stripped from dump-last-strategy, dump-last-briefing, strategy-utils, response-schemas, transformers, providers/consolidator, and providers/briefing.
- **Snapshot fidelity (Rule 11)** preserved — incoming `briefing-service.js` keeps `fetchEventsForBriefing({ snapshot })` at line 1280 as the per-snapshot event discovery path. No background `startEventSyncJob` reintroduced.
- **Unified AI Architecture (Rule 10)** strengthened — adapters/index.js wired through HedgedRouter; consolidator, weather-traffic-validator, holiday-detector, planner-gpt5, venue-intelligence migrated from direct provider calls to `callModel(role, …)`.
- **Model-agnostic adapters (Rule 14)** — anthropic/openai/gemini adapters use lazy init, accept `messages` arrays for chat history, and no longer hardcode model→key mappings.
- **ABSOLUTE PRECISION (6-decimal coords, Google place_id)** enforced — venue-address-resolver rounds Google API coords to 6 decimals; event-matcher rewritten to key on `place_id` then `venue_id` then name; hashEvent v2 uses `title|venue_name|city|date`.
- **Event canonical naming** — normalizeEvent and validateEvent use `event_start_date`/`event_start_time` per CLAUDE.md (2026-01-10).
- **NO FALLBACKS** — schemas.js adds `country` field instead of hardcoding "United States"; weather-traffic-validator removes hardcoded model.

## Special Cases

### `server/lib/infrastructure/job-queue.js` (DU — deleted by us, kept by them)
**Resolution:** KEEP (worktree already has incoming content; staged via `git add`).
**Rationale:** Per policy 1, checked usage. Still imported by `server/api/health/job-metrics.js` and `server/api/location/location.js` in incoming branch. Removing it would break those routes. Not tied to the legacy `startEventSyncJob` (that worker was removed 2026-02-17 per Rule 11 and was a different background mechanism).

### `server/config/assistant-policy.json` (UD — kept by us, deleted by them)
**Resolution:** DELETE (`git rm`).
**Rationale:** Per policy 2, the file already exists at `config/assistant-policy.json` (root config dir, in another agent's bucket). Removing the duplicate.

## Per-File Resolution Table

| Path | Type | Resolution | Rationale |
|------|------|------------|-----------|
| server/config/env-registry.js | AA | TAKE INCOMING | Model defaults bumped to `claude-opus-4-6` / `gemini-3.1-pro-preview` / `gpt-5.5-2026-04-23`; voice model corrected to `gpt-realtime` (was wrong-class). |
| server/config/load-env.js | AA | TAKE INCOMING | Simplified loader; adds GCP service-account credential reconstruction from individual env vars (Replit Secrets shape). Drops obsolete DEPLOY_MODE contract. |
| server/config/validate-env.js | AA | TAKE INCOMING | Adds JWT_SECRET/VECTO_AGENT_SECRET/Uber OAuth validation as prod errors; updates strategy-model defaults; allows test-mode skip. |
| server/config/assistant-policy.json | UD | DELETE | Duplicated at `config/assistant-policy.json` (root); removing this stale copy. |
| server/db/connection-manager.js | AA | TAKE INCOMING | Adds SSL conditional (prod), 57P01 transient retry wrapper, idleTimeout 10s→30s, `allowExitOnIdle: false`. Significant resilience improvement. |
| server/db/db-client.js | AA | TAKE INCOMING | LISTEN client SSL conditional + reconnect resubscribe channels + reset notification handler flag. Fixes orphaned SSE subscribers after reconnect. |
| server/jobs/change-analyzer-job.js | AA | AUTO-RESOLVED | Auto-merged (whitespace/docs only diff). Stages of HEAD content; both versions reference `docsOrchestrator` which exists. |
| server/lib/ai/adapters/anthropic-adapter.js | AA | TAKE INCOMING | Lazy init via `getClient()`; supports passing full `messages` array for chat history. |
| server/lib/ai/adapters/gemini-adapter.js | AA | TAKE INCOMING | F-002 thinkingLevel validation, vision/image support, native `systemInstruction`, JSON-mode + safety-OFF, citation suppression for google_search. |
| server/lib/ai/adapters/index.js | AA | TAKE INCOMING | HedgedRouter integration with concurrency-gate + error-classifier; per-role 503 retry with model swap; cross-provider fallback via registry. Rule 14 enforced. |
| server/lib/ai/adapters/openai-adapter.js | AA | TAKE INCOMING | Lazy init + dummy-key mock client for tests; supports `messages` array; supports gpt-5 family `max_completion_tokens`. |
| server/lib/ai/index.js | AA | TAKE INCOMING | Drops `runConsolidator` export (daily strategy removed), drops dead `models-dictionary.js`; exports `rideshareCoachDAL` from new module. |
| server/lib/ai/model-registry.js | AA | TAKE INCOMING | New roles: AI_COACH (renamed from COACH_CHAT), CONCIERGE_*, UTIL_TRANSLATION, OFFER_ANALYZER (+ DEEP), DOCS_GENERATOR; dead BRIEFING_EVENTS_VALIDATOR removed; gpt-5.2 → gpt-5.5-2026-04-23. |
| server/lib/ai/providers/briefing.js | AA | TAKE INCOMING | Reduced to thin compatibility wrapper for `runBriefing`; logic centralized in briefing-service.js. |
| server/lib/ai/providers/consolidator.js | AA | TAKE INCOMING | `runConsolidator` (daily) removed; `runImmediateStrategy` now uses callModel adapter; STRATEGIST_ENRICHMENT_PLAN integration (driver prefs, deadhead, traffic intel, weather forecast, event distance NEAR/FAR). |
| server/lib/ai/unified-ai-capabilities.js | AA | TAKE INCOMING | Bumps model to `claude-opus-4-6` (1-line diff). |
| server/lib/briefing/briefing-service.js | AA | TAKE INCOMING | Snapshot-fidelity event fetcher `fetchEventsForBriefing({ snapshot })` at line 1280 (Rule 11). +800 lines vs ours: title-similarity dedup, Places API venue resolution at discovery time, school-closure camelCase normalization at ingestion, FAIL HARD on subsystem errors, NO NULLS pipeline enforcement. |
| server/lib/briefing/dump-last-briefing.js | AA | TAKE INCOMING | Removes `consolidated_strategy` lines; adds timezone-aware timestamp formatting. |
| server/lib/briefing/filter-for-planner.js | AA | TAKE INCOMING | Adds `todayEvents` parameter for state-scoped pre-fetched events; NEAR/FAR distance bucketing for 15-mile rule (driver-relevant surge intelligence). |
| server/lib/briefing/index.js | AA | TAKE INCOMING | C-1 fix: export names match actual `fetchTrafficConditions`/`fetchEventsForBriefing`/`fetchRideshareNews`; replaces dead `event-schedule-validator` with `cleanup-events`. |
| server/lib/change-analyzer/file-doc-mapping.js | AA | TAKE INCOMING | 2026-02-17 safety fix: removes CLAUDE.md from gateway-server.js mapping so docs-agent never overwrites doctrine. |
| server/lib/events/pipeline/hashEvent.js | AA | TAKE INCOMING | Hash v2: `title\|venue_name\|city\|date` (was `title\|venue_address\|date\|time`). Fixes cross-city collisions; treats time corrections as same event (UPDATE not duplicate). |
| server/lib/events/pipeline/normalizeEvent.js | AA | TAKE INCOMING | Adds `cleanVenueName` + alias; smarter all-day/category-based time defaults; canonical `event_start_date`/`event_start_time` preserved. |
| server/lib/events/pipeline/validateEvent.js | AA | TAKE INCOMING | Schema v4: category required + fuzzy rescue via normalizeCategory; `today \|\| yesterday` date constraint (rejects future-dated noise). |
| server/lib/external/tts-handler.js | AA | TAKE INCOMING | Per-language voice selection (`nova` for non-English including European); accepts `language` ISO 639-1 param. |
| server/lib/infrastructure/job-queue.js | DU | KEEP (worktree already incoming) | Still used by `api/health/job-metrics.js` and `api/location/location.js`; not tied to legacy startEventSyncJob. Staged via `git add`. |
| server/lib/location/getSnapshotTimeContext.js | AA | TAKE INCOMING | Trusts pre-resolved `snapshot.dow/hour/day_part_key`; adds `toLocalTimeString` for real-UTC timestamps; documents local_iso vs real-UTC distinction. |
| server/lib/location/holiday-detector.js | AA | TAKE INCOMING | Timezone-aware cache key (no UTC date mismatch); local static detector + Easter Computus algorithm runs before AI call (zero cost, 100% reliable for US holidays); migrated to callModel adapter. |
| server/lib/location/weather-traffic-validator.js | AA | TAKE INCOMING | Migrated from direct callGemini to UTIL_WEATHER_VALIDATOR/UTIL_TRAFFIC_VALIDATOR roles via callModel (Rule 14). |
| server/lib/notifications/email-alerts.js | AA | TAKE INCOMING | Lazy Resend init with explicit warning when RESEND_API_KEY absent; alert/test-email no-op with descriptive return value when client unavailable. |
| server/lib/strategy/dump-last-strategy.js | AA | TAKE INCOMING | Removes `consolidated_strategy` debug section. |
| server/lib/strategy/planner-gpt5.js | AA | TAKE INCOMING | Migrated from direct fetch(OPENAI_URL) to callModel('STRATEGY_TACTICAL') (Rule 14); uses registry config for thinkingLevel/tokens/features. |
| server/lib/strategy/strategy-generator-parallel.js | AA | TAKE INCOMING | Reduced to legacy stubs returning `{ok:false, reason:'feature_disabled'}`. Active path is blocks-fast → consolidator → runImmediateStrategy. |
| server/lib/strategy/strategy-utils.js | AA | TAKE INCOMING | Removes daily-strategy comments from `isStrategyReady`; adds 1-hour POST_EVENT_SURGE_MS window in `isEventFresh` (rideshare-relevant pickup surge). |
| server/lib/strategy/tactical-planner.js | AA | TAKE INCOMING | P0-6: coordinates REMOVED from LLM schema; Google Places API resolves venue/staging coords post-LLM (single source of truth per ABSOLUTE PRECISION rule). Driver preference injection. |
| server/lib/traffic/tomtom.js | AA | TAKE INCOMING | Doc-only correction to `fetchRawTraffic` JSDoc (radiusMeters not radiusMiles). |
| server/lib/venue/enhanced-smart-blocks.js | AA | TAKE INCOMING | State-scoped event fetch with venue_catalog left-join (`fetchTodayDiscoveredEventsWithVenue`); event-time relevance parser supports both 24h and 12h AM/PM; catalog promotion writes back venue_id. |
| server/lib/venue/event-matcher.js | AA | TAKE INCOMING | Rewritten to use strong identity keys: place_id → venue_id → substantial-name. DB query moved up to caller. Eliminates fragile address-string matching that dropped metro-wide events. |
| server/lib/venue/index.js | AA | TAKE INCOMING | Drops `boostFromEvents` (event-proximity-boost.js dead code). |
| server/lib/venue/venue-address-resolver.js | AA | TAKE INCOMING | `searchPlaceWithTextSearch` exported with optional `radius` (50m default for venue lookup, 50km for event metro discovery); returns coords rounded to 6 decimals. |
| server/lib/venue/venue-cache.js | AA | TAKE INCOMING | State-only fuzzy lookup (was state+city, dropped cross-metro matches); defensive address fallback for NOT NULL safety; timezone+market_slug set on creation; address validator + Places API re-resolution when cached address fails validation. |
| server/lib/venue/venue-intelligence.js | AA | TAKE INCOMING | Migrated direct callGemini → callModel('VENUE_TRAFFIC') (Rule 14); openNow used as fallback only when canonical weekdayDescriptions absent (still warns on discrepancies). |
| server/lib/venue/venue-utils.js | AA | TAKE INCOMING | Re-exports `generateCoordKey` for backward compat (with comment to migrate to canonical coords-key.js). |
| server/logger/workflow.js | AA | TAKE INCOMING | Removes "Daily Strategy" reference; updates EVENTS provider note (sync-events.mjs → briefing-service.js → Gemini pipeline). |
| server/scripts/seed-market-cities.js | AA | TAKE INCOMING | Schema rename `us_market_cities` → `market_cities` (market consolidation 2026-02-17). |
| server/scripts/test-gemini-search.js | AA | TAKE INCOMING | Model bump `gemini-3-pro-preview` → `gemini-3.1-pro-preview`. |
| server/validation/response-schemas.js | AA | TAKE INCOMING | Removes `consolidatedStrategy` from BlocksFastGet schema; adds hours/quality/closed-go-anyway fields to VenueSchema. |
| server/validation/schemas.js | AA | TAKE INCOMING | Adds optional `country` to newsBriefingSchema (NO FALLBACKS — accept from client instead of hardcoding). |
| server/validation/transformers.js | AA | TAKE INCOMING | Removes `consolidated`/`consolidated_strategy` from `toApiBlocksResponse` and `toApiStrategyPolling`; adds venueId/beyondDeadhead/distanceFromHomeMi fields. |

## Files Needing Melody Review

None. Every conflict was resolved with a clear doctrine-aligned rationale. The incoming branch is the directionally-correct evolution; nothing required punting.

## Verification

```bash
# No remaining unmerged files in scope
git diff --name-only --diff-filter=U | grep -E "(server/(config|db|jobs|lib|logger|scripts|validation)/|server/lib/infrastructure/job-queue\.js)"
# (empty output)

# All assigned files staged
git status --short server/ | grep -E "^(AA|UU|UD|DU)"
# (empty output)
```

## Notes for Other Agents

- `server/jobs/triad-worker.js` (UU) is in `server/jobs/` but was **not in this agent's assignment** and is owned by another agent (likely the bootstrap/jobs agent). Untouched.
- `server/api/health/job-metrics.js` and `server/api/location/location.js` import `../../lib/infrastructure/job-queue.js` — kept that file intact for them.
- The `findOrCreateVenue`, `geocodeEventAddress`, `searchPlaceWithTextSearch`, `deduplicateEventsSemantic`, `loadDriverPreferences`, `buildDriverPreferencesSection`, `formatLocalTime`, `enrichVenueFromPlaceId`, `validateVenueAddress`, `resolveTimezoneFromMarket`, `validateEventsHard`, `getVenuesByType` cross-module imports were verified to exist in their respective source files before merging.
- `server/lib/ai/router/{hedged-router,concurrency-gate,error-classifier,index}.js` and `server/lib/ai/adapters/vertex-adapter.js` are required by the new `adapters/index.js` and were verified present.
